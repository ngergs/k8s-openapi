# Optionable fork

Fork for testing the [`optionable`](https://github.com/ngergs/optionable) crate.

It is `k8s-openapi` v0.26.0 with the `fake::Dummy` trait derived for all types.

Will be force pushed to updated major versionsßwhen `optionable` gets updated. Don't use it as a dependency.